{
  "name": "kaonui",
  "version": "0.1.0",
  "private": true,
  "description": "A modern component library with Lit and Vue wrappers",
  "type": "module",
  "packageManager": "pnpm@8.0.0",

  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "check": "turbo check",
    "test": "turbo test",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.4.1",
    "turbo": "^1.10.16",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}